clear all;
close all;

a = [0, 0.5, 1];
A = 5;
k = 0:2*A;
T = 40;
t = -100:0.1:100-0.1;
over = 10;

for i = k(4:-1:1)
    
    figure(i+1);
    for j = 1:3
        
        [phi(:,j, i+1), time] = srrc_pulse(T, over, A, a(j));
        plot(time-i*T, phi(:,j, i+1));
        title(['Phi(t-', num2str(i), 'T)']);
        hold on;
        
    end
    hold off;
end

for i